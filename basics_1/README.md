# Basics 1 - Networking

This project focuses on basic network configurations on an Ubuntu server, specifically managing host resolution using the `/etc/hosts` file.

## Tasks
* **0-change_your_home_IP**: A Bash script that configures `localhost` to resolve to `127.0.0.2` and `facebook.com` to resolve to `8.8.8.8`.
